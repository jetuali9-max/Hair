const KEY="SANJID_HAIR_V2",MS=86400000,M=[['Wash / scalp care','Follow your wash plan without over-washing.'],['Conditioner','Use conditioner after shampoo when washing.'],['Gentle handling','No rough towel rubbing or aggressive combing.'],['Low heat','Avoid unnecessary high heat / harsh styling.'],['Nutrition + water','Balanced protein-containing food + hydration.']];let S=load(),selected=today();function today(){return iso(new Date())}function iso(d){return new Date(d.getTime()-d.getTimezoneOffset()*60000).toISOString().slice(0,10)}function D(s){return new Date(s+'T00:00:00')}function load(){try{return JSON.parse(localStorage.getItem(KEY))||{startDate:today(),days:{}}}catch{return{startDate:today(),days:{}}}}function save(){localStorage.setItem(KEY,JSON.stringify(S))}function di(s){return Math.floor((D(s)-D(S.startDate))/MS)+1}function get(s){if(!S.days[s])S.days[s]={checks:[0,0,0,0,0],opened:false,saved:false,photo:null,review:null,note:'',time:null};return S.days[s]}function fmt(s){return D(s).toLocaleDateString(undefined,{day:'2-digit',month:'short',year:'numeric'})}
function tab(id){document.querySelectorAll('.screen').forEach(x=>x.classList.toggle('active',x.id===id));document.querySelectorAll('.nav').forEach(x=>x.classList.toggle('active',x.dataset.tab===id));if(id!=='home'){let d=get(today());d.opened=true;save()}render();window.scrollTo({top:0,behavior:'smooth'})}document.querySelectorAll('[data-tab]').forEach(x=>x.onclick=()=>tab(x.dataset.tab));document.getElementById('todayBtn').onclick=()=>tab('today');
function render(){let t=today(),d=get(t),i=di(t),elapsed=Math.max(0,Math.min(180,i-1));document.getElementById('dayNumber').textContent=Math.max(1,Math.min(180,i));document.getElementById('todayDate').textContent=fmt(t);document.getElementById('elapsedBar').style.width=elapsed/180*100+'%';document.getElementById('elapsedLabel').textContent=`Day ${i} of 180`;let score=Math.round(d.checks.filter(Boolean).length/5*100);document.getElementById('score').textContent=score;document.getElementById('orb').style.background=`conic-gradient(#69c4ac ${score*3.6}deg,#e8ebef 0deg)`;stats();missions(d);photo(d);calendar();details(selected);chart();document.getElementById('startDate').value=S.startDate;document.getElementById('note').value=d.note||'';document.getElementById('homeTitle').textContent=d.saved&&d.checks.every(Boolean)?'Today is complete ✦':'Your mission is waiting';document.getElementById('homeStatus').textContent=d.saved&&d.checks.every(Boolean)?'Nice. Keep the same consistency tomorrow.':'Open Today’s Mission and tick off what you actually did.'}
function stats(){let t=today(),c=0,m=0,u=0,p=0;for(let i=0;i<180;i++){let s=iso(new Date(D(S.startDate).getTime()+i*MS));if(s>t)continue;let d=S.days[s];if(!d||!d.opened){if(s<t)u++;continue}if(d.photo)p++;if(d.saved&&d.checks.every(Boolean))c++;else if(s<t)m++}complete.textContent=c;missed.textContent=m;photos.textContent=p;unopened.textContent=u}
function missions(d){let box=document.getElementById('missions');box.innerHTML='';M.forEach((x,i)=>{let l=document.createElement('label');l.className='mission';l.innerHTML=`<input type="checkbox" ${d.checks[i]?'checked':''}><div><b>${x[0]}</b><small>${x[1]}</small></div>`;l.querySelector('input').onchange=e=>{d.checks[i]=e.target.checked?1:0;d.saved=false;save();render()};box.appendChild(l)});document.getElementById('missionStatus').textContent=`${d.checks.filter(Boolean).length} / 5 completed`}
document.getElementById('saveDay').onclick=()=>{let d=get(today());d.opened=true;d.saved=true;d.time=new Date().toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});save();render();navigator.vibrate?.(25)};document.getElementById('saveNote').onclick=()=>{let d=get(today());d.opened=true;d.note=document.getElementById('note').value.trim();save();render()};
document.getElementById('photoInput').onchange=e=>{let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>{let d=get(today());d.opened=true;d.photo=r.result;d.time=new Date().toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});d.review=review(d);save();render()};r.readAsDataURL(f)};function review(d){let score=Math.min(100,58+d.checks.filter(Boolean).length*7+(d.note?3:0));return{score,text:score>=85?'Strong consistency today. Keep the same calm, controlled routine.':score>=70?'Good consistency. Keep handling gentle and avoid chasing extra volume.':'Lower-consistency day. Focus on the basics and restart tomorrow.',tips:['Use similar lighting and angle for future comparisons.','One photo cannot measure true density or growth reliably.','Condition lengths after shampoo if hair feels dry or fluffy.']}}
function photo(d){let p=document.getElementById('preview'),r=document.getElementById('review');if(!d.photo){p.classList.add('hidden');r.classList.add('hidden');return}p.classList.remove('hidden');p.innerHTML=`<img src="${d.photo}" alt="SANJID progress photo">`;r.classList.remove('hidden');r.innerHTML=`<strong>${d.review.score}/100</strong><p><b>Photo review:</b> ${d.review.text}</p><ul>${d.review.tips.map(x=>`<li>${x}</li>`).join('')}</ul>`}
function calendar(){let c=document.getElementById('calendar');c.innerHTML='';let t=today();for(let i=0;i<180;i++){let s=iso(new Date(D(S.startDate).getTime()+i*MS)),d=S.days[s],b=document.createElement('button');b.textContent=i+1;if(d){if(d.saved&&d.checks.every(Boolean))b.classList.add('complete');else if(d.opened&&s<t)b.classList.add('partial');else if(s<t)b.classList.add('unopened');if(d.photo)b.classList.add('photo')}if(s===selected)b.classList.add('selected');if(s>t)b.disabled=true;b.onclick=()=>{selected=s;details(s);calendar()};c.appendChild(b)}}
function details(s){let box=document.getElementById('details'),d=S.days[s],i=di(s);if(!d){box.innerHTML=`<p class="muted">No record for Day ${i} (${fmt(s)}).</p>`;return}let status=d.saved?(d.checks.every(Boolean)?'Complete':'Partial / missed'):d.opened?'Opened, not saved':'Not opened';box.innerHTML=`<div class="grid"><div class="detail"><span>DAY</span><b>${i} • ${fmt(s)}</b></div><div class="detail"><span>STATUS</span><b>${status}</b></div><div class="detail"><span>TIME</span><b>${d.time||'—'}</b></div><div class="detail"><span>PHOTO</span><b>${d.photo?'Added':'No photo'}</b></div></div><div class="detailnote"><b>Checklist:</b> ${d.checks.map((x,j)=>`${x?'✅':'⬜'} ${M[j][0]}`).join(' • ')}</div><div class="detailnote"><b>What I was doing:</b> ${d.note||'No note recorded.'}</div>${d.photo?`<img class="detail-photo" src="${d.photo}">`:''}${d.review?`<div class="detailnote"><b>Photo review:</b> ${d.review.score}/100 — ${d.review.text}</div>`:''}`}
function chart(){let c=document.getElementById('chart'),ctx=c.getContext('2d'),w=c.clientWidth||500,h=230,dpr=devicePixelRatio||1;c.width=w*dpr;c.height=h*dpr;ctx.scale(dpr,dpr);let max=Math.max(1,Math.min(180,di(today()))),L=28,R=9,T=15,B=27,cw=w-L-R,ch=h-T-B;ctx.font='9px system-ui';ctx.fillStyle='#9aa2ae';[0,25,50,75,100].forEach(v=>{let y=T+ch-v/100*ch;ctx.strokeStyle='#eceef2';ctx.beginPath();ctx.moveTo(L,y);ctx.lineTo(w-R,y);ctx.stroke();ctx.fillText(v,L-23,y+3)});let last=null;for(let i=0;i<max;i++){let s=iso(new Date(D(S.startDate).getTime()+i*MS)),d=S.days[s];if(!d){last=null;continue}let val=d.checks.filter(Boolean).length/5*100,x=L+i/Math.max(1,max-1)*cw,y=T+ch-val/100*ch;if(last){ctx.strokeStyle='#7564d8';ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(last.x,last.y);ctx.lineTo(x,y);ctx.stroke()}ctx.fillStyle=d.saved&&d.checks.every(Boolean)?'#65bba5':'#d6a05e';ctx.beginPath();ctx.arc(x,y,3,0,Math.PI*2);ctx.fill();if(d.photo){ctx.strokeStyle='#7564d8';ctx.beginPath();ctx.arc(x,y,5,0,Math.PI*2);ctx.stroke()}last={x,y}}}
document.getElementById('changeStart').onclick=()=>{let v=document.getElementById('startDate').value;if(v&&confirm('Update the 180-day journey start date?')){S.startDate=v;save();render()}};document.getElementById('reset').onclick=()=>{if(confirm('Delete all tracker data, notes and photos from this browser?')){localStorage.removeItem(KEY);S=load();selected=today();render();tab('home')}};window.addEventListener('resize',chart);render();